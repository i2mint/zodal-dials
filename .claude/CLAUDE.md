# zodal-dials

**The agent dev guide for this repo lives in [`../AGENTS.md`](../AGENTS.md) — read it first.** It is
the index/map: what zodal-dials is, the architecture rules you must respect, the Zod v4 gotchas, the
dev-skills table (`/zodal-dials-dev-*`), the spawned agents (`.claude/agents/`), the
skill-maintenance loop, and the key docs (`docs/dev-plan.md`, `docs/zodal-dials-concept.md`,
`docs/research/`).

This file is a pointer only; all behavioral content is maintained in `AGENTS.md` so it stays
agent-agnostic. Do not duplicate content here.

zodal-dials is the **settings/configuration/preferences specialization of zodal**, part of the
`_zodals` ecosystem (`_zodals/.claude/CLAUDE.md`).
